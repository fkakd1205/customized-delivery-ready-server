package com.customized_delivery_ready.server.service;

import java.util.List;

import javax.transaction.Transactional;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.context.TestConfiguration;
import org.springframework.test.context.junit.jupiter.SpringExtension;

// @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.MOCK)
// @ExtendWith(SpringExtension.class)
// @Transactional
public class ExcelTranslatorHeaderServiceTest {
    
    // @Autowired
    // private ExcelTranslatorHeaderTestRepository excelTranslatorHeaderTestRepository;

    // @Test
    // public void 엑셀변환기_헤더상세_조회() {
    //     System.out.println(excelTranslatorHeaderTestRepository.findAll());

    //     System.out.println("##### ExcelTranslatorHeaderServiceTest.엑셀변환기_헤더상세_조회() 성공 #####");
    // }
}
